// JSON 파일을 import합니다. TypeScript에서 이를 위해 "resolveJsonModule" 옵션이 활성화되어야 합니다.
import libraryLintConfig from "./library"
import nextLintConfig from "./next"

// JSON 설정을 export합니다.
export { libraryLintConfig, nextLintConfig }
