import ibjPackage from "./packages"

const { baseConfig, nextConfig, reactLibraryConfig, libraryLintConfig, nextLintConfig } = ibjPackage

export { ibjPackage as default, baseConfig, nextConfig, reactLibraryConfig, libraryLintConfig, nextLintConfig }
